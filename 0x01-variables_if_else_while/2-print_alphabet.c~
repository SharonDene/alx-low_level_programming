#include <stdio.h>

/**
 *main - display alphabet letter in lowercase
 *
 *return: Always 0 (Sucess)
 */
int main (void)
{
	int ch;
	char = "abcdefghijklmnopqrstuv";

	for (ch = ‘a’; ch <= ‘z’; ch++)
	{
		putchar (ch);
	}
	{
		putchar (‘\n’);
		return (0);
	}
		
